{% extends '@Admin/base.html.twig' %}

{% form_theme form 'bootstrap_3_layout.html.twig' %}

{% block h1 %}Photo new{% endblock %}

{% block content %}
    <div class="row">
        <div class="col-md-offset-3 col-md-6">
            {{ form(form) }}

            <ul class="record_actions list-unstyled">
                <li>
                    <a class="come-back" href="{{ path('admin.category.index') }}">
                        Back to the list
                    </a>
                </li>
            </ul>
        </div>
    </div>
{% endblock %}
